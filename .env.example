VITE_SUPABASE_URL=https://datkqtnredzlwttlxuie.supabase.co
VITE_SUPABASE_ANON_KEY=
# Pastor's auth.users id — used by /public/* pages to scope reads to
# the single pastor's data without requiring login. Get it from the
# Bulletin App's /admin/users page (click the user-id chip to copy).
VITE_PASTOR_USER_ID=
